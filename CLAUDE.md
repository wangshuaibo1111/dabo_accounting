# 大博记账 — 产品文档

## 📌 项目基本信息

- **项目名称**：大博记账
- **项目定位**：个人日常记账应用，专注于支出记录与管理
- **目标平台**：Windows 10/11 + macOS 11+
- **货币单位**：人民币（¥ / CNY）
- **技术栈**：Electron + React + TypeScript
- **开发原则**：简单实用、界面直观、数据安全

---

## 🎯 核心功能需求

### V1.0（首版）
| 功能 | 描述 |
|------|------|
| 📝 记录支出 | 输入金额、选择日期、选择分类、填写备注 |
| 📂 两级分类 | 一级大类 + 二级小类，灵活选择 |
| 📋 账单列表 | 按时间倒序查看所有支出记录 |
| 🔍 筛选查询 | 按日期范围、分类筛选账单 |
| 💾 本地存储 | 数据保存在本地，无需联网，保障隐私 |
| 🗑️ 删除记录 | 支持删除单条支出记录 |

### 未来可扩展（不纳入首版开发）
- 月度/年度统计报表
- 图表可视化（饼图、折线图）
- 收入记录
- 预算设置与管理
- 数据导出（Excel/CSV）
- 云备份同步
- 多账户管理

---

## 📂 支出分类体系

### 完整分类表

| 一级大类 | 图标 | 二级小类 |
|---------|------|---------|
| 餐饮饮食 | 🍽️ | 三餐、零食、水果、饮品、外卖、聚餐 |
| 交通出行 | 🚗 | 公交地铁、出租车、网约车、加油充电、停车费、火车机票 |
| 购物消费 | 🛒 | 衣服鞋帽、日用百货、数码产品、美妆护肤、家居用品 |
| 住房居家 | 🏠 | 房租、房贷、物业费、水电燃气、维修装修、日用品 |
| 医疗健康 | 💊 | 门诊、药费、住院、体检、牙科 |
| 教育培训 | 📚 | 书籍、课程、考试费、文具 |
| 休闲娱乐 | 🎮 | 电影、游戏、旅游、运动健身、KTV |
| 通讯网络 | 📱 | 话费、宽带、流量 |
| 人情往来 | 🎁 | 红包礼品、请客吃饭、慈善捐款 |
| 金融保险 | 💰 | 保险、手续费、理财 |
| 其他支出 | 📦 | 临时支出 |

---

## 🏗️ 技术架构

### 最终方案：React SPA + sql.js（浏览器运行）

> 📝 **变更记录**：
> 1. 最初选择 Electron，因 Windows 上已知 Bug（[GitHub Issue #49034](https://github.com/electron/electron/issues/49034)）放弃
> 2. 改选 Tauri v2，因网络环境无法连接 crates.io（Rust 包仓库）放弃
> 3. **最终方案**：纯 Web 应用，所有依赖通过 npm（npmmirror）获取，零原生编译

| 层面 | 技术 | 说明 |
|------|------|------|
| 前端框架 | React 18 + TypeScript | 构建用户界面 |
| 样式方案 | Tailwind CSS 3 | 原子化 CSS |
| 数据库 | sql.js（SQLite → WebAssembly） | 纯 JS 实现的 SQLite，无需原生编译 |
| 数据持久化 | localStorage | 数据库文件序列化为 Base64 存储 |
| 构建工具 | Vite 6 | 快速前端构建 |
| 包管理 | npm（npmmirror 镜像） | 依赖管理 |
| 运行方式 | 浏览器（开发）/ 可部署为静态站点 | 跨平台，Windows + Mac 均可使用 |

### 为什么最终选择纯 Web 方案？

| 约束 | 解决方式 |
|------|---------|
| Electron `require("electron")` Bug | ❌ 无已知修复 |
| Cargo 网络不通 | ❌ 所有镜像被拦截 |
| npm 可用 | ✅ 使用 npmmirror 镜像 |
| sql.js（WASM） | ✅ 纯 npm 包，零原生依赖 |

### 关于桌面体验

当前版本通过浏览器运行。未来可选的桌面化方案：
- 使用 **PWA**（Progressive Web App）：可从浏览器"安装"到桌面，离线可用
- 等待 Electron Bug 修复后迁移
- 使用 **Tauri**（需解决网络问题后）

---

## 📐 数据模型设计

### 支出记录 (Expense)

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | string (UUID) | ✅ | 唯一标识 |
| amount | number | ✅ | 金额（元），精确到分（两位小数） |
| categoryL1 | string | ✅ | 一级大类 |
| categoryL2 | string | ✅ | 二级小类 |
| date | string (YYYY-MM-DD) | ✅ | 支出日期 |
| note | string | ❌ | 备注，最大200字 |
| createdAt | string (ISO 8601) | ✅ | 记录创建时间 |
| updatedAt | string (ISO 8601) | ✅ | 记录最后修改时间 |

---

## 🖥️ 界面设计概要

### 主界面布局
```
┌─────────────────────────────────────┐
│  大博记账            [设置] [关于]   │  ← 顶部导航栏
├─────────────────────────────────────┤
│  [+ 记一笔]                         │  ← 快捷操作区
├──────────┬──────────────────────────┤
│ 日期筛选 │  账单列表                 │  ← 左侧筛选 + 右侧列表
│ 分类筛选 │  ┌───────────────────┐  │
│          │  │ 2024-08-04        │  │
│  📅 今天 │  │ 🍽️ 三餐  ¥25.00  │  │
│  📅 昨天 │  │   食堂午饭         │  │
│  📅 本周 │  ├───────────────────┤  │
│  ...    │  │ 2024-08-03        │  │
│          │  │ 🚗 地铁  ¥6.00   │  │
│          │  │ ...              │  │
│          │  └───────────────────┘  │
└──────────┴──────────────────────────┘
```

### 记一笔弹窗
```
┌──────────────────────┐
│  记一笔              │
│                      │
│  金额：[_100.00__]   │
│  日期：[2024-08-04]  │
│  分类：[餐饮饮食 ▾]   │
│  小类：[三餐 ▾]       │
│  备注：[___________]  │
│                      │
│  [取消]    [保存]     │
└──────────────────────┘
```

---

## ⚙️ 技术决策规则（核心规则）

> **📋 技术决策规则**
>
> 在整个项目开发过程中，涉及以下事项时，开发者**必须先列出 2-4 个方案**，用通俗易懂的语言向用户解释各方案的优劣势，**由用户做出最终决定**，开发者不得自行决定：
>
> - 技术选型（框架、库、工具）
> - 架构设计（项目结构、模块划分）
> - UI/UX 设计方向（布局、交互方式）
> - 功能实现方式（多种可行方案时）
> - 第三方服务/依赖的选择
>
> **原因**：用户不懂编程技术，无法提供技术层面的具体要求。开发者需承担技术顾问的角色，但决策权归用户所有。

---

## 📁 项目结构

```
大博记账APP/
├── CLAUDE.md              # 本产品文档
├── package.json           # 前端依赖与脚本
├── vite.config.ts         # Vite 构建配置
├── tsconfig.json          # TypeScript 配置
├── tsconfig.node.json     # TypeScript Node 配置
├── tailwind.config.cjs    # Tailwind CSS 配置
├── postcss.config.cjs     # PostCSS 配置
├── index.html             # HTML 入口
├── src/                   # 源代码
│   ├── main.tsx          # React 入口
│   ├── App.tsx           # 根组件（状态管理、布局）
│   ├── index.css         # 全局样式 + Tailwind 指令
│   ├── components/       # UI 组件
│   │   ├── AddExpenseDialog.tsx  # "记一笔"弹窗
│   │   ├── ExpenseList.tsx       # 账单列表
│   │   └── FilterBar.tsx         # 筛选栏
│   ├── lib/              # 业务逻辑
│   │   └── database.ts   # 数据库操作（sql.js）
│   ├── data/             # 静态数据
│   │   └── categories.ts # 分类定义
│   └── types/            # TypeScript 类型
│       └── index.ts      # 公共类型定义
└── dist/                  # 构建输出
```

---

## ✅ 开发阶段划分

| 阶段 | 内容 | 状态 |
|------|------|------|
| 1 | 产品文档编写 & 技术栈确认 | ✅ 完成 |
| 2 | 项目初始化（经历 Electron → Tauri → Web 三次调整） | ✅ 完成 |
| 3 | 分类数据定义 & sql.js 数据库层 | ✅ 完成 |
| 4 | UI 组件开发（记账弹窗、账单列表、筛选栏） | ✅ 完成 |
| 5 | 功能完善 & 测试 | 🔄 进行中 |
| 6 | 桌面化方案（PWA / Electron 修复后迁移 / Tauri） | ⏳ 待进行 |

---

> 📅 文档创建日期：2026-08-04
> 📝 最后更新：2026-08-04
